[package]
name = "responses-stream"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
async-openai = { path = "../../async-openai", features = ["responses"] }
tokio = { version = "1", features = ["full"] }
futures = "0.3"
serde_json = "1"
