[package]
name = "responses-stream"
version = "0.1.0"
edition = "2024"

[dependencies]
async-openai = { path = "../../async-openai" }
tokio = { version = "1.0", features = ["full"] }
futures = "0.3"
serde_json = "1.0"
