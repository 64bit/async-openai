[package]
name = "completions-stream"
version = "0.1.1"
edition = "2021"
publish = false

[dependencies]
async-openai = {path = "../../async-openai"}
futures = "0.3.26"
tokio = { version = "1.25.0", features = ["full"] }
